# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ImageSaverAndDisplayer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImageSaverAndDisplayer
 pod 'Kingfisher', '~> 5.0'
pod 'Alamofire'
pod 'SVProgressHUD'
  target 'ImageSaverAndDisplayerTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
